#include <stdio.h>
#include <stdlib.h>

int main()
{
    int x, y, r;

    printf("enter the value of x: \t");
    
    scanf("%d", &x);
    printf("enter the value of y: \t");
    
    scanf("%d", &y);

    r = x+y;
    printf("The Result of x+y is: %d", r);

        return 0;
}
